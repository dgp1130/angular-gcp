{
  "name": "ng-gcp-backend",
  "version": "0.0.0",
  "scripts": {
    "start": "npm run -s build && node ../../dist/backend/server.js",
    "build": "tsc -p tsconfig.server.json",
    "dev": "tsc -p tsconfig.server.json --watch & nodemon --watch ../../dist/backend/ ../../dist/backend/server.js"
  },
  "private": true,
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1"
  }
}
